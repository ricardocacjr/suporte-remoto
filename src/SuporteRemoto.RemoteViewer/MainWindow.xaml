<Window x:Class="SuporteRemoto.RemoteViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SuporteRemoto.RemoteViewer"
        mc:Ignorable="d"
        Title="SuporteRemoto - Visualizador" Height="450" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sessão remota" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Status: " VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText" Text="Desconectado" VerticalAlignment="Center" FontWeight="Bold" />
        </StackPanel>

        <TextBox Grid.Row="2" x:Name="LogBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                  TextWrapping="Wrap" FontFamily="Consolas" />
    </Grid>
</Window>
